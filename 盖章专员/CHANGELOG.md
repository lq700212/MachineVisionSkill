# 版本更新记录

## v1.2.0 (2026-09-02)

### Excel 工作簿支持：多一步"转 PDF"，之后盖章管线零差异

真实场景：报价单/订购单等合同常以 .xlsx 交付。按用户要求实现"Excel 先转 PDF
再走现有盖章逻辑"——与人工在 Office 中「另存为 PDF」打通为同一件事（第 0 步
转换，定位/盖章/压平全部复用文本版 PDF 管线，不新写任何定位逻辑）。

- 第 0 步输入类型新增 `excel`（xlsx/xls/xlsm/xlsb）：调用本机 Excel COM
  （win32com，DispatchEx 独立进程）执行 `ExportAsFixedFormat`，等价于 Office
  「另存为 PDF」——导出全部可见工作表，保留原打印设置/分页/打印区域
- **src_pdf 必须重新指向转换后的 PDF**（曾漏改：src_pdf 仍指原 .xlsx，
  pdfplumber 把 zip 当 PDF 解析报 "No /Root object"，被误当"导出异步未写完"
  排查较久——铁律1教训，注释已固化）
- 导出落盘防御性等待：`ExportAsFixedFormat` 正常同步写完；保留"pdfplumber 能
  打开且页数≥1"才放行的兜底（防杀软实时扫描/慢磁盘锁）。⚠️ 探测不能用 fitz：
  MuPDF 对不完整 PDF 宽容（缺 /Root 也肯开），pdfplumber 严格校验 xref 才与
  后续管线一致
- 输出固定在 `.xlsx` 同目录 `原名_已盖章.pdf`；默认输出名以**原 Excel 文件名**
  为基准（orig_input），不受临时 PDF 路径替换影响
- 依赖登记：`pywin32`（win32com）加入 `OPTIONAL_DEPENDENCIES`（仅 Excel 输入
  需要；缺失自动装、装不上只影响 Excel 输入）
- 转换失败诚实降级：打印"手动用 Office 另存为 PDF 后把 PDF 交给盖章"兜底指引，
  绝不静默

### 验收基线（本机 Excel 2013 COM 实测）

- `E:\Agent工作空间\盖章\` 两份报价单 xlsx 全流程盖章 PASS：转 PDF（文本版）→
  关键词定位"买方签字/盖章："右侧 → 压平 → 文字可搜（"盖章/签字"、"苏州华际
  光电"均命中）→ 章红像素重心 vs 预期偏差 <3pts（实测 (440,609) vs 预期
  (441.1,612.1)，另一份偏差同量级）
- 回归基线 `tests/run_test4_scan.py` 24/24 PASS（文本/扫描件/图片三线无回归）
- `--dry-run` / 清理逻辑对 Excel 输入正常

## v1.1.1 (2026-09-02)

### 扫描件关键词定位三连修（实测"工伤保险待遇申领表"扫描件定位落空 → 命中"盖章"二字）

真实案例：3 页扫描件申领表，第 2 页底部"单位（盖章）/承诺人（签名）："签名栏，
旧版关键词定位必落空到默认右下角。排查发现根因是**三个独立缺陷叠加**，缺一修不可：

1. **OCR 检测器丢行**：rapidocr det 默认 `box_thresh=0.5`，签名栏整行检测框
   被丢弃，OCR 文字层根本没有这行 → `_lazy_full_ocr_engine()` 改
   `det_box_thresh=0.3` + `det_unclip_ratio=2.0` 提高召回（注意：新版
   rapidocr_onnxruntime 传任何 det_* 参数必须显式带 `det_model_path=None`，
   否则 KeyError: 'model_path'）
2. **文字层词宽失真**：`_insert_invisible_lines()` 按行高推字号，文字横向铺开
   =字号×字数，与 OCR 行框宽脱节（213pts 的行框被铺成 496pts）→ 写入前用
   `fitz.get_text_length` 把词宽缩放到与 OCR 行框一致，定位/搜索才反映真实版式
3. **正文行过滤阈值过紧**：`find_keyword_blocks()` 的 `max_word_width` 200pts
   把 213pts 的签名栏整行误当正文过滤 → 放宽到 300（正文贯穿行通常 ≥380pts，
   仍可有效区分）

### 骑压精对齐：章骑在"盖章"关键词上，而不是长行几何中心

长签署行（如"单位（盖章）/承诺人（签名）："）触发骑压时，旧版章中心骑整块
几何中心——实际盖在"承诺人"附近，偏了半个行宽。现在按字符均布估算关键词在
块内的位置（文字层词宽已校准，见上），章中心精对齐到命中的关键词（"盖章"）
二字处；找不到关键词明细（智能定位合成块）时退回块中心原行为。
`find_keyword_blocks` 的 block 新增 `kw_words` 命中词明细字段承载此能力。

### 验收

- 真实扫描件：关键词定位成功，章中心对准第 2 页"（盖章）"处（红像素中心实测
  (282.0, 603.1) vs 期望 (283.1, 606.0) pts）；压平防编辑通过（1 张整页位图）、
  文字层可搜索（"单位（盖章）"可搜到）
- 回归基线：`tests/run_test4_scan.py` 24/24 PASS

## v1.1.0 (2026-09-01)

### 三类输入统一支持：文本版 PDF / 扫描件 PDF / 图片（拍照件）

**设计核心：输入规范化，而非新写定位逻辑**。扫描件/图片先转换为"整页位图 +
OCR 隐形文字层"的规范化 PDF（与压平同一套 render_mode=3 技术），之后智能定位/
角色匹配/禁忌区避让/骑压/压平**零改动复用原管线**——所有已有策略、决策表、
防编辑能力对三类输入自动生效。

- **扫描件 PDF**：每页 get_text 不足 20 字符判定为无文本层 → 150dpi 渲染每页 →
  整页 OCR（rapidocr **带 det** 模式，按行识别；与公章 rec-only 是两种用途，
  不可混用）→ 逐行写入隐形文字层（整行一次 insert_text 保行结构，签署方正则
  "需方：A 供方：B"同行多对匹配依赖此）→ 输出 PDF 文字反而可搜索（增强）
- **图片输入**（png/jpg/jpeg/bmp/webp）：规格化为 595pts 宽页面（否则拍照大图
  按 1px=1pt 会让默认章宽占比严重失调）→ OCR 文字层 → 盖章压平后**渲染回与原图
  同分辨率、同格式的图片**（章融入像素，不可移除）；显式指定 .pdf 输出则给 PDF
- **OCR 依赖缺失诚实降级**：扫描件/图片没有 OCR 就完全没有文字 → 明确警告
  "定位必落默认位置，交付前必须人工核对"并给出 pip 安装命令，不阻塞盖章
- **输出覆盖防护**：输出路径与合同文件/公章图片路径相同时拒绝执行
  （测试事故曾把默认公章图片当输出覆盖销毁，git 恢复后固化此防线）
- 输出路径决策：图片输入未指定输出 → `原名_已盖章.原扩展名`；PDF 输入 →
  `原名_已盖章.pdf`（不变）

### 验收基线

- 扫描件/图片专项 24/24 PASS（`盖章测试/run_test4_scan.py`：文本版回归、
  扫描件 OCR+智能定位+压平+文字可搜、图片默认输出同格式同分辨率、图片转 PDF、
  dry-run、落点像素级校验——红章在"乙方（盖章）"行附近且未漂移默认右下角；
  测试脚本与原固件生成器 make_fixture.py 本次重建，原 run_test*.py 已随旧
  工作区丢失）
- 全流程目检：`%TEMP%\seal_test_out\` 下目检图
- 测试脚本已沉淀到 skill/tests/，不再污染用户工作区

## v1.0.0 (2026-08-31)

> 本 skill 从未对外发布，此前的内部迭代记录已合并为本版本——首个正式版本。

### 核心能力

**智能定位（默认开启）：给一枚公章就知道盖哪**
- OCR 识别公章上的公司名：红色像素定位圆心 → 极坐标展开把环形公司名拉直成横排
  （顶行=外半径）→ 4 个起始角分别识别+投票（防展开缝切断文字）→ 红像素垂直投影
  裁主文字行 → rec-only 按字间隙分段识别拼接 → 清洗截断到组织后缀 + 质量评分选最优
- 自动匹配合同角色：逐页正则提取"角色: 公司名"签署方对（甲方/乙方/丙方/丁方/
  卖方/买方/供方/需方/出租方/承租方/发包方/承包方/委托方/受托方/招标人/投标人/
  借款人/贷款人/用人单位/劳动者），与公章公司名模糊匹配（互相包含或相似度 ≥0.55，
  容忍 OCR 单字误差）
- 定位该角色盖章处：搜索"该角色（盖章/公章）"文字块（带盖章字样避免误匹配抬头
  当事人栏），章左缘对齐文字块右缘
- 任何一步失败只警告并逐级回退常规定位（参考章对称 → 关键词 → 默认位置），绝不阻塞
- 人工兜底通道：`--company "公司名"`（OCR 不准）、`--role 角色名`（跳过 OCR 与匹配）、
  `--no-auto`（完全关闭智能定位）

**防编辑压平（默认开启）：公章与页面融为一体**
- 盖章页整页栅格化（默认 300dpi JPEG q92）+ 重建隐形文字层（render_mode=3，
  文字仍可搜索/复制，显示效果不变）
- 公章从此是页面像素的一部分，PDF 编辑软件（Acrobat/福昕/WPS 等）无法单独选中、
  移动、抠走、删除——杜绝法律风险
- `--no-flatten` 可退回叠加方式（公章=独立图片对象），仅限应急/内部核对
- 其余页原样保留，页数页序不变；`--dpi` 控制清晰度/体积

**常规定位三模式**（智能定位失败时的回退链）
- seal：检测到已有公章（近正方形 50~250pts 的图片），左右对称放置
- keyword：无公章时搜索"签章/公章/盖章"关键词（过滤正文长行+近邻聚类），章左缘
  对齐关键词文字块右缘
- default：全部失败的明示兜底（右下角），有警告输出，交付前需人工核对

**真实合同适配（5 份真实单据实测验收，测试固化于 run_test3_real.py，35/35 PASS）**
- 四级定位策略（find_role_stamp_block）：①明确盖章栏（"需方(签章)："——盖章/公章/
  签章 × 全半角括号全组合）②裸签章栏+角色列对齐（订购单"签章： 签章："式，排除
  其他角色盖章栏，绝不盖到对方栏）③**对侧对称**（本角色无盖章栏但有他角色盖章栏：
  章与他角色盖章栏同高、落在本角色关键字列——报价单卖方章盖"卖方开户行"行、与
  "买方签字/盖章"上下对齐，用户口述规则实测校准）④公司名同行兜底（输出带"建议
  人工确认"提示）
- **骑压式盖章**：签署文字块宽≥章宽 90% 时，章中心骑在文字块上（模拟真实盖章
  压字）——修复"需方(签章)：XX公司"长行外排把章推出文本区贴页边（购销合同实测
  用户指出）；另加页面边界保护（所有模式通用，越界自动收进并打印警告）
- 签署方提取适配真实合同：一行多对"角色：公司名"（字符级 negative-lookahead 截断）、
  角色带"名称/单位"修饰（"买方名称："）、空白公司名栏不误吞下一段、纯数字（电话/
  账号）不混入
- 每类真实单据的验收基线（角色/策略/章落点区域）固化为预期表，一条命令复判
- **落点禁忌区动态判断层**（build_forbidden_zones + adjust_avoid_forbidden）：
  现场计算"他角色盖章栏+他角色公司名"禁忌区，落点相交自动网格搜最近合法位——
  同模板不同布局的文档自适应兼容，不依赖逐例打补丁；扫描件（无文本层）自动降级
- 定位阈值全部相对页面尺寸（同高水平带=页高7%、列距=页宽35%），横版/异形页兼容

**弱模型等效固化（用户明令：每个功能点、每次改动都要考虑，无需用户提醒）**
- AGENTS.md 铁律9：判断机检化（脚本输出给出下一步动作）、流程照抄化（SKILL.md
  输出→动作决策表）、兜底提示→照做级、测试一条命令出 PASS/FAIL
- SKILL.md 新增「标准作业流程」：命令模板 + 7 种脚本输出对应的行动决策表 + 固定
  交付话术——弱模型照抄即可达到与强模型一致的作业质量

**尺寸精确匹配**：从 PDF 提取参考公章原图，红色检测裁出内容区域，按内容占比换算
实际视觉大小（pts）放置新章——不受图片透明 padding 影响

**公章图片通用处理**：PNG(RGBA/RGB)、JPG/JPEG 全支持；RGB 自动红色检测转透明
背景，避免白底遮挡合同文字；裁掉透明 padding 只留印章内容

**盖章效果**：默认逆时针旋转 20° 模拟真实盖章，`--rotate`/`--offset-x`/`--offset-y`
可调；`--dry-run` 只分析不生成

**依赖自动安装**：脚本启动自检，缺失依赖自动经阿里镜像源安装；核心依赖装不上才
退出并给手动命令；OCR 属可选依赖，装不上自动降级不影响盖章。新增第三方依赖必须
登记脚本顶部 `REQUIRED_DEPENDENCIES` / `OPTIONAL_DEPENDENCIES` 表

### 关键实现教训（详版见 AGENTS.md 历史事故档案）

- RapidOCR 的 det 检测器对公章大字彻底失灵，必须走 `use_text_det=False` 的 rec-only
- rec 输入宽度被压到 ~320px，整行十几字会丢字 → 必须分段；等宽硬切会把字切成两半
  （"光电"→"光中"）→ 必须在字间隙处下刀
- 极坐标展开轴向写反会整行镜像（顶行必须=外半径）
- pdfplumber/PyMuPDF 顶部原点 vs reportlab 底部原点，坐标系混算是定位第一大坑
- 叠加层公章是独立图片 XObject，可被编辑软件选中移动——这就是必须压平的根因

### 验收基线

- 常规定位+压平回归 10/10 PASS（`盖章测试/run_test.py`）
- 智能定位专项 17/17 PASS（`盖章测试/run_test2.py`：卖方/买方/乙方匹配、
  --role/--company 兜底、OCR 失败回退、智能定位路径下压平仍生效）
- 真实合同 35/35 PASS（`盖章测试/run_test3_real.py`，test/ 下 5 份真实单据：
  订购单列对齐、购销合同明确盖章栏、报价单公司名同行兜底）
- 目检：订购单章落"需方代表/签章"列与对方章并排；购销合同落"需方(签章)"右侧；
  报价单落卖方名称行右侧（无盖章栏场景，已带人工确认提示）
