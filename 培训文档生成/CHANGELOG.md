# 更新日志

`培训文档生成` skill 的所有版本变动统一记录在此。格式参考 Keep a Changelog。

## [Unreleased]

- 新增 SKILL.md §四 MES 分层写法（自助闭环）：操作员版只写"不用管＋保证批号/SN 扫对"；客户版给完整自助链（要厂方拿 5 样 → Mock 对格式 → 开真发 → 配置字典 → 字段词汇表 → 排障三步 → 何时找我方）；内部版才写代码边界（哪 5 种情况必须改代码、改哪）；协议类参数字典与客户版自助章双向对账。
- 新增 SKILL.md §五 MES 自助闭环核验项＋§六两版大纲同步（客户版自助闭环章、内部版 MES 节"改配置够/必须改代码"边界＋甩给客户的附件名）。
- 修复 SKILL.md §十一核验脚本禁词误杀：`dev` 子串匹配会误杀本站字段 `device`，`发码/后门` 保留子串匹配，账号类 `dev` 改走词边界正则 `\bdev\b`（忽略大小写）。
- 新增 SKILL.md §R2 harness 视觉样式前置（2026-09 实证）：`Main()` 开头调 `EnableVisualStyles()＋SetCompatibleTextRenderingDefault(false)` 与产品一致，否则标准控件按 Classic 渲染；判据为 harness 图与真机实拍逐像素 diff 分区看（标题栏区 mean>15 即中招）。
- 新增 SKILL.md §R2 拍前 `f.Activate()`＋主窗口按真实行为铺满：非激活标题栏灰蓝与使用态不一致；固定小尺寸会挤掉标题栏按钮。
- 新增 SKILL.md §R2 静态对话框优先真进程实拍（最高优先级）：harness 直 new 对话框恒为 classic 边框（根因未完全查明，逐项对照排除后定为经验结论），静态窗一律真进程驱动，harness 只拍无硬件演不出的动态态。
- 新增 SKILL.md §R2 驱动真进程严禁 `SendMessage(BM_CLICK)`（2026-09 现场事故）：同步调用遇模态框卡死发送线程，一律 `PostMessage(BM_CLICK)＋WaitTitle`；配套输入框按屏幕坐标排序一次填对、看门狗只关非目标小弹窗、FAIL 当场截全屏、单实例预检、全英文日志、finally 验退、登录密码先读配置哈希确认默认值。

## [v1.0.0] - 2026-09-14（首次入库）

- 三份制全流程首次入库：操作员版/客户技术工艺版/内部版定位差异矩阵＋五步工作流（盘点→实拍→分段写→核验→同步周边）。
- 截图核心：R1 生产路径定值源＋R2 按窗体二选一（PrintWindow/真屏）＋R3 非空校验探针＋防卡住三层（Dispose 不 Close/看门狗/续拍）＋tools/DocShot 固定路径。
- 弱模型照抄执行：§七脚本化总览 9 步＋§八探针/§九看门狗/§十定值源/§十一 check_docs.py/§十三 md_to_pdf.py 模板＋分段写作≤4KB 红线＋新项目适配清单。
